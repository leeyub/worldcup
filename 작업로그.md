# 작업로그

세션 종료("push해줘") 시 맨 위에 한 항목씩 추가. **최신이 위.**

## 2026-06-12
- **패착 분석 문장 자연스럽게 재작성 (dev)**: 모듈 조합 결과 명사로 끝나던 문장('빗나감' 등)·조사 오류를 전부 완결 문장으로. 은/는·을/를 josa 헬퍼 추가, `scorerWhy`·`why`맵·`pickRisk`·`actualNote` 종결어미 통일(~어요/~죠/~네요/~답니다). (런타임 LLM 호출은 클라이언트 키노출·과금 이슈로 부적합 → 빌드타임에 템플릿 다듬음)
- **리포트/배점/코멘트/토스트 보완 (dev)**: ①상세 리포트 **한글 조사 자동 보정**(받침 유무로 이/가·이라/라·이었/였·으로/로 — '체코이→체코가' 등 어색함 해소) ②'신의 한 수' 초록 박스 **높이 균일**(min-height+1줄 말줄임) ③**배점 규칙 카드 항목별 줄나눔·점수 위계** + 경기 전 **상단으로 이동** ④**경기 중 한 줄 코멘트 저장 버그 픽스**(upsert `onConflict` 누락이 원인) ⑤**재진입 시 그동안 바뀐 점수 토스트 한 번에**(localStorage 스냅샷 비교, `replayMissed`).
- **참여자 이모지 옵션 동물로 통일·확장 (dev)**: 닭🐔·원숭이🐵·돼지🐷(불쾌 가능) + 물건 이모지(⚽🚀👑 등) 제거, **동물 39종**으로 확장(곰·여우·판다·토끼·너구리·사슴·기린·말·거북·고슴도치·수달·펭귄·새류·돌고래·고래·문어·용·공룡 등). 자동 아바타(AVATARS)도 🐵🐷→🐰🐮로 정리. 기존 저장된 이모지는 표시 유지.
- **기본 탭 고정 + 미제출 문구 제거 (dev)**: ①참여자 카드 미제출 옆 '(판돈은 여러분께 기부!)' 삭제 ②앱 진입 기본 탭을 **자동 다음경기 이동(`curMatchIdx`) → 고정 상수 `HOME_TAB`(현재 'm2')**로 변경. 경기 끝나도 자동으로 안 넘어감 — 오너가 메인 화면 명시 요청 시에만 변경.
- **어드민 모달 스크롤 튐 전면 수정 (dev)**: 경기 탭(1/2/3차전)·참석 토글·등록·삭제 등 패널 내부 버튼이 `adminPanel()` 재호출로 스크롤 0으로 튀던 것 → `adminRerender()`(스크롤 위치 유지 재렌더)로 통일.
- **참석 후속 보완 (dev)**: ①신규 등록자는 **이미 끝난 경기 자동 미참석**(addPart→finished 경기 absent 추가) ②앱 진입 시 **현재 경기(2차전) 탭 기본 노출**(`S.tab=MATCHES[curMatchIdx()].id`) ③어드민 참석 토글 시 **모달 스크롤 위치 유지**(맨 위로 튀던 것). ④1차전은 이미 locked+finished라 배팅 불가·아카이브(순위/리포트만) — 확인 완료(코드 불필요).
- **경기별 참석/미참석 (dev)**: 경기마다 참석자가 달라지는 문제 해결. `match_state.absent`(미참석 id 배열, opt-out) 도입 → 미참석자는 그 경기의 **참여자 카드·미제출 목록·인원수·순위·리포트·정산에서 자동 제외**. 어드민 패널에 **🙋 경기별 참석** 카드(경기탭+참석/미참석 토글, `attendCard`/`toggleAttend`). **점심값 정산을 경기별(참석자만)로 변경**(로스터 다른 경기 꼬임 방지). 종합 순위는 미참석 경기 '·' 표시. ⚠️**DB에 `absent` 컬럼 추가 SQL 필요**. (※다른 세션 미완성 `logEvent`도 working tree에 있어 함께 커밋됨 — 죽은 코드)
- **리포트/점심값 보정 (dev)**: ①점심값 분배 **엔빵+등수차등** 모델로 변경(꼴등 부담↓: 49,000→38,500, `BILL_STEP` 튜닝 가능) + 카드에 상세 분배 로직 설명 추가 & 갱신 보조타이틀 제거 ②상세 리포트 **편집디자인 개선**(단락 `.repsec` 간격, 서체 통일, 초록 '신의 한 수'를 `.rephit` 박스로) ③클러스터 **참여자끼리 중복 안 되게 유니크 배정**(`clusterCandidates`+`assignClusters`, 8명 전원 다른 별명).
- **개인 리포트 → 이 경기 순위에 병합 (dev)**: 중복되던 별도 `개인 리포트` 카드 제거. **경기 종료 시** '이 경기' 순위 행에 클러스터 배지 표시 + **행 클릭 → 상세 리포트** 모달. 라이브 중엔 기존 항목별 점수 펼침 유지. 안내 문구도 종료 시 '이름 누르면 📑 상세 리포트'.
- **개인 리포트 강화 (dev)**: ①클러스터 12→**22종**으로 대폭 다양화(전지적 작가 시점·타이밍의 마술사·난타전 신봉자·0:0 수문장·반쪽 셜록 등) ②상세 리포트를 **한 페이지 서사형**으로 — 🗣️총평+S~F 등급, 🧭배팅 성향 분석, 🎬무슨 일이 있었나(예측vs실제 타임라인), ✨신의 한 수, 💥패착 분석(항목별 재미 사유), 🔮다음 판 조언.
- **개인 경기 리포트 기능 (dev)**: 경기 종료 시 `개인 리포트` 카드(등수·클러스터 배지·점수) + 이름 클릭→상세 모달(유쾌한 클러스터명+배팅 성향+✨신의 한 수+💥패착 원인 `내 예측→실제`). 예측 vs `getFacts` 자동 계산(`reportData`/`clusterOf`/`reportCard`/`showReport`). 1차전 실데이터로 검증(8명 5클러스터로 분화).
- **점심값 내기 배팅 결과 카드 (배포)**: 총 196,000원을 **등수 가중치(1등 0·2등 1·…·꼴등 N-1) 비례**로 차등 분배(1등 0원). 8명 기준 0/7천/…/49천 = 196,000 정확. 동점=평균가중치(같은 금액), 100원 단위·잔액은 꼴등 보정. 한 경기라도 끝나면 종합 순위 기준 자동 표시(`betSplit`/`betCard`, `BILL_TOTAL` 상수). 결과(순위) 아래 노출 + 분배 로직 설명 포함.
- **토스트 디자인 디벨롭 (dev)**: 색 초록/빨강 → **파랑(획득)/빨강(감점)**, 솔리드 배경 → **반투명(.8~.88)+블러(글래스)+소프트 섀도**.
- **데모 이펙트 미리보기 버튼 + 종료 플래시 일치 (dev)**: 데모 패널에 "이펙트" 행(⚽골/🟨카드/🏁종료 플래시 + 🎉토스트 따다닥(+감점 빨강) 버튼)으로 신규 이펙트 즉시 확인. 실제 경기 종료(`confirmFinish`)·관전자 폴링에도 🏁 플래시 추가 → 데모=실동작 일치.
- **UI 검수 6차 반영 (dev, 운영 미반영)**: ①"화면은 5초마다 갱신" 문구 제거(라이브 힌트 2곳·푸터 — 이제 변화 시에만 재렌더라 부정확) ②**어드민 패널 "참여자별 예측" 카드 추가**(경기 탭 + 제출자별 전체 픽 표시, 마감 전에도 확인 가능; `fmtPred`/`adminPredsCard`). bun 문법 검증 통과.
- **UI 검수 5차 반영 (dev, 운영 미반영)** — 오너 피드백 일괄:
  - 예측폼 **후반전 시간도 TV식 0~15분 표기**(`HBAND`/`bandTxt`, band 값·채점 불변), 비교보드도 '전반/후반 N분'으로 일관.
  - **토스트 감점 핑크→붉은색**(`.toast.bad`, hue-rotate 제거), **여러 토스트 미세 시간차(70ms) 따다닥**.
  - **골·카드 입력 시 즉각 상황 플래시**(`eventFlash`, 중앙 팝업): 어드민 입력 즉시 + 관전자는 5초 폴링이 새 골/카드 감지 시. 토스트 직전.
  - **라이브 중 참여자 카드 숨김 + 라이브 카드 최상단**(`!L.on` 조건).
  - **탭 가로스크롤 세로흔들림 방지**(`overflow-y:hidden`+`touch-action:pan-x`), **탭 이동 시 활성탭 자동 가운데**(`centerActiveTabs` — 스크롤 원복 버그 해소).
  - **티커 끊김 근본 수정**: `refresh()`가 5초마다 무조건 재렌더→티커 애니 0부터 재시작이 원인(데모 아닌 실상용도 동일했음). **변화 없으면 재렌더 스킵**(`liveSig` 비교) → 티커·탭스크롤·펼친 행 유지.
  - **힌트 문장단위 줄바꿈**(예측입력·예측마감·라이브채점 + 동일 패턴 어드민/관전자).
  - bun JS 문법 검증 통과.
- **다음 할 일**: `/demo` 라이브 상태로 플래시·카드순서·티커 검수 → OK면 "배포해줘". DESIGN.md에 신규 컴포넌트(eventFlash·toast.bad·livechip·sheet·subtab·티커) 반영은 미결.

## 2026-06-11
- **UI 검수 4차 반영 + 운영·dev 동시 배포** — 운영본 피드백:
  - **사이트 기본 테마 라이트**로 변경(FOUC 스크립트 default 'dark'→'light', 저장값 있으면 유지).
  - 참여자 그룹 라벨(제출완료/미제출) 앞 이모지 제거.
  - 예측폼 첫 골 시간대: **전반전/후반전 소제목**으로 분리(0~15·16~30·31~45+ / 46~60·61~75·76~90+).
  - **어드민 패널 시트 모드** 적용(예측폼과 동일 — 딤/배경겹침·스크롤 이슈 해소).
  - 직원등록 행: 전화 라벨 📱→**한글 '전화'**, 사번/전화 범례 정렬(`.tk` 고정폭).
  - 순위 상세(breakdown) **상대팀 국기** 표시(matchRows에 mid 전달→'상대 첫 득점자'→'🇨🇿 첫 득점자').
  - 예측 비교 단독표시: **🦄 제거**(텍스트 '단독'만), **골드 아웃라인 제거**(`.bgroup.solo` 삭제).
  - 한 줄 코멘트 카드 버튼 '저장'→**'변경'**, 로그인 버튼 🛡️ 제거. (+직전 3차 상단 spacing 보정 동봉: topbar 12px·header 30px)
- **UI 검수 3차 반영 + 운영 배포 (dev→main)** — 운영본 보며 받은 피드백:
  - 최상단 spacing 추가(topbar 패딩 18→34px), 로그인 팝업 타이틀↔서브타이틀(겹침→8px)·입력창↔버튼(8→28px) 간격 확보.
  - **LIVE 뱃지**: 크기 축소(11→10px)·**캡슐 radius(999px)**·**경기시간 연동**(`liveNow` = 라이브ON + 현재시각≥킥오프 → 킥오프 전엔 미노출).
  - **이호민 🦅 운영 미반영 버그 픽스 + 근본원인**: 직전(`37851dd`)에 이모지를 **데모 데이터(`DEMO_PARTS`)에만** 넣어 운영(DB 참여자)엔 반영 안 됨 → `avatarOf`가 DB emoji 없을 때 `AVATARS[순번]`(이호민=🐸)로 폴백하던 게 원인. **`NAME_EMOJI` 맵을 `avatarOf`에 추가**해 운영·데모 공통 적용(DB emoji 우선). 동일원인(데모전용) 전수점검 → 서체·성뺀이름은 이미 공통코드라 추가 누락 없음.
- **UI 검수 2차 반영 (dev, 집)** — 오너 디자인 피드백 일괄 처리:
  - *spacing/radius*: 최상단 타이틀 위 여백+노치(safe-area) 보강, 첫 카드 위 여백(minfo mb 18px), 카드 우측 배지(`.pt`) 라운드 8px, **Apple식 코너 스무딩**(`corner-shape:superellipse(4)` `@supports`로 — Chrome만, Safari/구형은 일반 라운드 폴백).
  - *라이트 선택강조*: 미선택/선택 대비 약해서 선택 토큰을 블루(`--sel`/`--selbd`)로 강화.
  - *이름 표기*: 비교보드·순위·토스트·코멘트를 **성 뺀 이름**(이동엽→동엽)으로. (참여자 로스터는 풀네임 유지) — `gname/gpname` 헬퍼+두글자성 예외.
  - *순위*: **동점 동일순위**(1,1,3…) `tieRanks`, `+N점 가능` 제거, 종합행은 차수별 점수를 총점 아래 구분선+정렬(`.permatch`).
  - *예측 비교 보드 재구성*: '빨간테두리=나혼자' 안내 삭제. **카테고리 내부 탭**(경기결과/스코어/첫득점자/…9종) + **같은 예측끼리 그룹핑**(인원순, 단독픽은 🦄로 자연 분리), 결과 확정 시 정답 초록·오답 흐림.
  - *한 줄 코멘트*: 별도 카드로 분리 + **본인 것은 경기 중에도 수정 가능**(`saveComment`, 마감 후에도 코멘트만 upsert) + 마감 후 **상단 롤링 티커**.
  - *라이브 채점*: 🔴 LIVE 표시를 **칩**으로(탭/헤더, 펄스 점), 종료는 그린 칩.
  - *예측폼 모달*: 딤 오버레이 ugly 문제 → **시트 모드**(배경=본문색 풀스크린, 헤더 sticky+구분선+safe-area). **옵션 셀렉터 높이 50px 통일**, MVP 등 select 높이 54px. **배점 표시 105→300 정합화**(폼이 옛 점수로 남아있던 버그 수정: 60/70/40/40/15/30/15/15/15).
  - *정리*: 미사용 CSS(`.brow/.bchip/.det`) 제거, 탭 스크롤바 숨김, `.claude/` gitignore.
  - 라이트/다크 × 여러 상태 브라우저 검증 완료.
  - *마무리(이어받기, 다른 세션 커밋 위에)*: 기본 서체 **SUIT** 적용(Pretendard CDN·데모토글은 비교용 유지), 테마 토글을 🌙 이모지→**SVG 해/달 아이콘+34×34 컨테이너**(클리커블)로, 참여자 이모지 이동엽→🦁·이호민→🦅(데모 데이터; 운영은 어드민이 DB에서 이호민 emoji 설정 필요), 카드 내 탭(순위 `이 경기/종합`·비교보드 카테고리)을 박스형→**언더라인 스타일**로(옵션 셀렉터와 시각 구분). 예측 jsonb 구조 무변경 교차검증(운영본과 키 동일) 후 **운영(main) 머지·배포**.
- **UI 검수 1차 반영 (dev, 집)**: ①참여자 카드 완료/미제출 그룹 분리 + 칩 라운드 10px·체크를 작은 초록 `✓`로 ②카드 헤더→콘텐츠 간격 12px 통일·서브타이틀 12px ③라이브 '시작' 버튼 어드민 전용(참여자 미노출) ④상단 탭 1/2/3차전 균등너비·종합 탭 제거 ⑤`순위` 카드 신설(참여자 아래, 내부 탭 `이 경기 | 종합`)로 기존 종합화면+경기순위 통합 ⑥비교보드 헤더에 경기 라벨(`1차전 예측 비교`)로 per-match 명시 ⑦**폰트 Pretendard·SUIT 둘 다 CDN 로드 + 데모 폰트 토글** (기존엔 `font-family`에 Pretendard만 적혀 있고 실제 로드 안 돼 Apple 기본폰트로 보이던 상태였음 → 결정 후 한쪽만 남길 예정). 데모 전 상태 브라우저 검증 완료.
- dev 브랜치 GitHub 최초 푸시 (+ HTTPS 토큰을 osxkeychain에 등록 → 이후 인증 자동).
- 데모 진입 경로 추가: **`/demo`** (`vercel.json` rewrite + index.html `pathname` 감지). 기존 `?demo`도 유지. 운영 배포 완료.
- Vercel 도메인 정리:
  - 운영: **`skt35-worldcup.vercel.app`** (기존 `worldcup-six-xi`도 유지 — 같은 운영 가리킴)
  - dev: **`dev-skt35-worldcup.vercel.app`** (Preview→`dev` 브랜치 연결, 기존 `dev-worldcup-six-xi`도 유지)
- 작업 흐름 약속 추가: "pull해줘" / "push해줘" (CLAUDE.md "작업 흐름" 섹션 참조).
- **백로그 1번 라이트 테마 1차 구현 (dev, 운영 미반영)**: `:root[data-theme="light"]` 웜그레이 배경 / 흰 카드 / 소프트 섀도 팔레트 + 상단바 🌙/☀️ 토글(localStorage 저장, **기본 다크**) + 다크전용 색(탭 활성·모달 오버레이·경고문) 라이트 보정.
- **라이트 테마 2차 보정 (dev, 피드백 반영)**: ①팔레트 채도↓·중립 밝은 톤(웜크림→뉴트럴 그레이 `#f3f4f6`) ②선택강조를 `--sel` 토큰으로 통합(앞으로 한 곳에서 제어) ③데모바도 `var(--card)`로 토큰화해 라이트 반영 ④카드/옵션/탭/칩/리더보드 패딩 확대(시원하게) ⑤푸터 하단 패딩 ⑥비교보드 `.brow` 디바이더 spacing 확대.
- **라이트 테마 3차 보정 (dev)**: ①골드 액센트 디골드 — 선택 테두리를 `--selbd` 토큰으로 분리(라이트=중립 회색 `#aeb4bf`), 선택 틴트 `--sel`도 라이트에선 뉴트럴로 ②포인트 텍스트 골드 톤 보정(`#d4880c`, 더 또렷). ⇒ 노란 아웃라인/칙칙한 텍스트 해소.
- **배점 105→300 변경 (`scorePred` + 표시 문구 + CLAUDE.md)**: 결과60·스코어70(30·15)·첫득점자 각40·첫골팀15·시간대30(15)·전반15·카드15·MVP15 = 300. 3경기 합산 900. **예측 데이터 호환 영향 없음**(점수는 채점 시 계산).
- **카드 색(옐로/레드)·받은 팀 구분 검토 → 미적용 권고**: 현재 카드는 "총 장수 구간(적음/보통/많음)" 단순 버킷 예측. 색·팀 구분 추가 시 예측 jsonb 구조 변경=기존 제출 호환 깨짐 + 입력/채점 복잡도↑. 친구용 게임엔 과함. 오너가 원하면 별도 작업으로.
- **카드 색/팀 구분: 미적용 확정** (오너 동의 — 단순 버킷 유지).
- **탭 위계 개편 A안 구현 (dev)**: ①현재 경기(끝나지 않은 첫 경기, `curMatchIdx`) 자동 판별→큰 탭 강조(`.tab.cur`) ②이후 경기 흐림+🔒(`.tab.soon`), 클릭 시 "곧 열려요+D-day" 예고 화면(`comingSoonView`, 미노출 X) ③종합 탭: 경기별 순위→종합 순위 순서 ④순위 FLIP 애니는 현재 경기 화면(`#lb`)만. `MATCHES`에 `kickoff` ISO 추가(D-day용). bun으로 JS 문법 검증 통과.
- **DESIGN.md 초안 작성 (dev)**: Google Material DESIGN.md 섹션 구조(Overview/Colors/Typography/Layout/Elevation/Shapes/Components/Do's & Don'ts). 현재 다크 토큰=현황, 라이트=의도로 정리. **토큰 원본=CSS `:root`, 문서=의도(intent)** 명시. 오너(디자이너)가 직접 다듬을 초안.
- **DESIGN.md 기본기 보강 (dev, M3 참고)**: Typography→M3 역할 스케일(display/headline/title/body/label) 매핑 + 줄높이 1.2×/1.5× 권장, Layout에 4/8px 스페이싱 스케일 + 그리드 이탈값(6·10·13·14·18) 정리표, Elevation 레벨표(0~3+오버레이), Colors 접근성·대비(골드 대비 주의), Components 상호작용 상태(state layer). m3.material.io 검색·참고.
- **DESIGN.md 기준 코드 정렬 (dev)**: 문서에 제안만 있던 4/8 스페이싱 스케일을 실제 `:root` 토큰(`--sp-1`~`--sp-8`)으로 구현하고 구조 spacings(카드·탭·옵션·칩·순위행·비교행·`.wrap`·푸터)에 적용 → 그리드 이탈값(13·14·9·10) 수렴. `.hint` 줄높이 1.5 보강. DESIGN.md도 "제안→구현됨"으로 동기화. (잔여 이탈: 데모바·스테퍼 등 추후)
- **다음 할 일 / 미결**: ①**폰트 최종 택1**(Pretendard vs SUIT — demo 토글로 검수 후) → 한쪽 CDN 제거 ②LIVE를 시계기준 자동노출까지 갈지(현재는 어드민 라이브 시작=실경기시간 기준, 칩만 적용) ③`+N점` 대신 'AI 1등 확률 %' 재미요소 도입 여부(보류) ④`DESIGN.md`에 신규 토큰/컴포넌트(순위 subtab·비교보드 group·ticker·sheet·livechip) 반영. → 데모 검수 → OK면 "배포해줘".
